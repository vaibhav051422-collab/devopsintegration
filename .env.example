# Local development values only (non-secret defaults for challenge setup).
# Do not use these values in production.

# Backend service settings
MONGO_URL=mongodb://db:27017/metadata_db
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ROOT_USER=dev_minio_user
MINIO_ROOT_PASSWORD=dev_minio_password

# Optional backend runtime port
PORT=5000
