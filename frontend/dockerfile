
FROM node:22-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm install

# Copy the rest of the project
COPY . .

# Match the port Vite uses
EXPOSE 5173

# Start Vite with the host flag
CMD ["npm", "run", "dev"]